{
    "_copy_without_render": [
        ".gitignore",
        ".pre-commit-config.yaml"
    ],
    "project_namespace": "awslabs",
    "hyphen_namespace": "{{ cookiecutter.project_namespace.lower().replace(' ', '-').replace('_', '-') }}",
    "underscore_namespace": "{{ cookiecutter.hyphen_namespace.lower().replace('-', '_') }}",
    "project_domain": "Short Domain",
    "hyphen_domain": "{{ cookiecutter.project_domain.lower().replace(' ', '-').replace('_', '-') }}",
    "underscore_domain": "{{ cookiecutter.hyphen_domain.lower().replace('-', '_') }}",
    "description": "An AWS Labs Model Context Protocol (MCP) server {{ cookiecutter.project_domain }} expert",
    "instructions": "Instructions for using this MCP server. This can be used by clients to improve the LLM's understanding of available tools, resources, etc. It can be thought of like a 'hint' to the model. For example, this information MAY be added to the system prompt. Important to be clear, direct, and detailed.",
    "author_name": "Your Name",
    "author_email": "githubusername@users.noreply.github.com",
    "python_version": ">=3.13",
    "version": "0.0.0"
}
